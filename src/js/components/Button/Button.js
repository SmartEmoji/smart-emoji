import React, { PureComponent } from 'react';

import './Button.scss';

class Button extends PureComponent {
  render() {
    return (
      <div
        className="Button"
      >
        Click Me!
      </div>
    );
  }
}

export default Button;
